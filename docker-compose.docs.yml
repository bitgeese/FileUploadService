services:
  docs:
    image: file_upload_service_local_docs
    container_name: file_upload_service_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./file_upload_service:/app/file_upload_service:z
    ports:
      - '9000:9000'
    command: /start-docs
